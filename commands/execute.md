---
description: Execute a Flutter implementation plan in batches with verification checkpoints
argument-hint: "[plan file path (default: latest in docs/plans/)]"
---

Use the flutter-executing skill exactly as written.

Invoke: `Skill tool: flutter-craft:flutter-executing`
